# bulma-python3-flask-starter
A Flask app using Bulma starter with basic authentication

# Development
```
conda create -n kadisttv python=3.8
pip install -r requirements.txt
```
default account:

`default`/`password`
